pub(crate) fn main(a: i32, b: i32) -> i32{
    let result_number;
    result_number = a + b;

    // Declara a variavel
    let mut a_number;

    // Declara a segunda variavel e associa um valor
    let a_word = "Ten";
    // Associa um valor para a primeira variavel

    a_number = 15;

    println!("the number is {}.", a_number);
    println!("the word is {}.", a_word);

    return result_number;
}
